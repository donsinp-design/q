version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - invoice_data:/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/data/invoices.db
      - PORT=3000
    restart: unless-stopped

volumes:
  invoice_data:
